# API base URL (without trailing slash)
#
# Production (Vercel):
# EXPO_PUBLIC_API_URL=https://invoice-mobile-app.vercel.app
#
# Real device + local backend:
# Use your computer's IP: EXPO_PUBLIC_API_URL=http://192.168.1.x:3000
# Find IP: ipconfig getifaddr en0 (Mac) or ipconfig (Windows)
# Phone and computer must be on same WiFi
#
# Emulator: localhost works (default)
